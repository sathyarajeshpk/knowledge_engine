# microsoft-fabric — artifact coverage

<!-- Generated by `ke index`. Rebuilt on every harvest. -->

**No artifacts yet**, across 270 knowledge object(s).

Artifacts are produced on demand and never by the scheduled run (ADR-0004). See `ke generate list`.
